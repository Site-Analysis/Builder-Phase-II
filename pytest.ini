[pytest]
testpaths = tests
python_files = test_*.py *_smoke.py *_validation.py
asyncio_mode = auto
markers =
    integration: marks tests that make real external HTTP calls (deselect with -m "not integration")
