# Node
node_modules/
dist/
.vite/
.env
.env.local
.env.*.local
.env.production
.env.production.*
!.env.example
secrets/
secrets/*.json
*.pem
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Claude Code: gitignore credential files only.
# Team-shared agents, skills, commands, and settings.json are committed.
.claude/mcp.json
.claude/settings.local.json
.claude/projects/
.claude/cache/

# Secrets
/services/service-account.json
gee-sa.json

# IMD test download artifacts
stn/

# Graphify knowledge-graph output (local tool artifact)
graphify-out/

# Large fallback data blobs — download-on-setup, NOT committed (reproducible open data).
# Fetch/verify via scripts/fetch_fallback_data.py; provenance in
# services/geo/app/data/FALLBACK_SOURCES.md. The small wards file (2.9 MB, ODbL) is NOT
# ignored — it stays committable.
/services/geo/app/data/lgd_villages.geojson
